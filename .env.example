# Groq API Configuration (Optional)
# Get your free API key at: https://console.groq.com/keys
# Without this key, the chatbot uses pattern-based responses only (still works great!)
# With this key, the chatbot can handle edge cases and complex questions using LLM
VITE_GROQ_API_KEY=your_groq_api_key_here

# Password is stored as SHA-256 hash in UnderConstruction.tsx
# To change password: echo -n "YourNewPassword" | sha256sum
